---

**Prompt to paste into Gemini CLI:**

You are now hardening the execution layer of Aether.

We just completed a powerful Think Engine (`core/think/engine.js`) with a 7-phase pipeline, multi-intent detection, and plan generation.

**Current problem:** The engine generates good plans, but the interpreter does not reliably execute them.

**Your task:**

Strengthen the execution side so that every plan generated by `think` actually runs correctly.

Specifically:

- Update `core/runtime/interpreter.js` so it can properly execute plans that use any of the 7 stdlib modules (`metabolism`, `witness`, `identity`, `memory`, `hire`, `swarm`, `evolve`).
- Ensure `receipt` is automatically generated and signed when a task completes.
- Add robust error handling — if a step fails, the interpreter should gracefully handle it and log the failure.
- Make sure `birth`, `ethics`, and `permission` are respected during execution.
- Improve how stdlib modules are imported and called from generated plans.

After making the changes, update the following:
- `examples/think-complex.aether` to test a full multi-module workflow
- `TODO.md` — mark this execution hardening as complete and list remaining work

Work step-by-step. Show the key code changes you made to `interpreter.js`.

Focus only on making execution reliable. Do not change the Think Engine itself.

Begin now.

---
